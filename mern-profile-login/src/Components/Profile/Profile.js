import React from "react";
import "./Profile.css";

export default function Profile({setLoginUser}) {
    return <div>
        <h1>Profile</h1>
        {/*<button onClick={()=>setLoginUser({})}>Logout</button>*/}
        <button >Logout</button>
    </div>
}